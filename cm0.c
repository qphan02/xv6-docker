#include "types.h"
// #include "user.h"
#include "cm.h"

int
main (void) {
	printf(1,"Starting...\n");
	// char *path = "contain_spec.json";
	// int maxproc = 10;

	// int ncreate =  cm_create_and_enter();
	// int nsetroot = cm_setroot(path, strlen(path));
	// int nmaxproc = cm_maxproc(maxproc);

	// printf(1,"cm_create_and_enter: %d\n", ncreate);
	// printf(1,"cm_setroot: %d\n", nsetroot);
	// printf(1,"cm_maxproc: %d\n", nmaxproc);

	// printf(1,"n1: %d\n", cm_create_and_enter());

	exit();
}